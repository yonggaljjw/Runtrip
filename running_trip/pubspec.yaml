name: running_trip
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.0
  latlong2: ^0.9.0  

  cupertino_icons: ^1.0.2
  http: ^1.2.0   # ← 내가 추가하라고 한 패키지는 여기로!

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/seoul.png
    - assets/busan.png
    - assets/daegu.png
    - assets/jeju.png
